export const NUMBER_OF_ANIMES_TO_DISPLAY = 10;
export const STARTING_RANK = 1;

// This is an estimation of how many animes existing in MAL database
// I cannot confirm at the moment due to the fact that there is no API from MAL that list how many animes they have
// The actual number may be a lot bigger
export const TOTAL_NUMBER_OF_ANIMES_EXISTING = 3000;
